# =====================================================================
# PRIMARY CREDENTIALS (REQUIRED)
# =====================================================================
# These credentials act as the application's identity. 
# They are required to authenticate with the Qobuz API.
QOBUZ_APP_ID=
QOBUZ_APP_SECRET=

# User Authentication: Enter your Qobuz token here.
QOBUZ_USER_AUTH_TOKEN=
# Note: QOBUZ_TOKEN is also supported for backward compatibility.
# QOBUZ_USER_ID is optional; it will be automatically detected if left blank.
QOBUZ_USER_ID=

# =====================================================================
# SPOTIFY INTEGRATION (OPTIONAL)
# =====================================================================
# If provided, the application will fetch enhanced metadata from Spotify,
# including Genre, BPM/Tempo, and Popularity metrics.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# =====================================================================
# DOWNLOAD SETTINGS
# =====================================================================

# Target directory for saved files. Default: 'downloads'
DOWNLOAD_PATH=downloads

# Number of concurrent downloads.
# Higher values increase speed but require more system resources and bandwidth.
MAX_CONCURRENCY=4

# ---------------------------------------------------------------------
# NAMING CONVENTIONS (FOLDERS & FILES)
# ---------------------------------------------------------------------
# Available placeholders:
# {artist}       = Artist Name
# {album}        = Album Title
# {title}        = Track Title
# {track_number} = Track Number (01, 02, etc.)
# {year}         = Release Year
# {quality}      = Audio Quality (e.g., Hi-Res 24bit-96kHz)

# Directory Structure:
# Use a forward slash (/) to create subdirectories.
# Example: {artist}/{album} -> Result: Artist/Album Name/
FOLDER_TEMPLATE={artist}/{album}

# Filename Convention:
# Example: {track_number} {title} -> Result: 01 Track Name.flac
FILE_TEMPLATE={track_number} {title}

# =====================================================================
# METADATA & ASSETS (LYRICS & ARTWORK)
# =====================================================================
# Metadata is automatically embedded within the audio files. 
# Use these settings to generate additional standalone files.

# Save a standalone lyrics file (.lrc)? (true = Yes, false = Embedded only)
SAVE_LRC_FILE=false

# Save the album cover as a separate image file (.jpg)?
SAVE_COVER_FILE=false

# Embed artwork directly into the audio file metadata? 
# Recommended for compatibility with most media players.
EMBED_COVER_ART=true

# Embed lyrics directly into the audio file metadata?
EMBED_LYRICS=true

# =====================================================================
# MISCELLANEOUS
# =====================================================================
# Configure a proxy URL if required for regional access or network restrictions.
PROXY_URL=